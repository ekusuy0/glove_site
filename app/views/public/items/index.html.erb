<h1>商品一覧画面</h1>
<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.item_image, size: "200x200" %>
    <%= item.name %>
  <% end %>
  <%= item.price %>円<br>
<% end %>